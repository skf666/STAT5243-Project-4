# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.venv/
venv/
env/
.env

# Jupyter
.ipynb_checkpoints/
*.ipynb_meta

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Project — large data and derived artefacts
# (Keep small fixtures in test_data/; do NOT commit raw scrapes or processed parquet.)
data/raw/
data/cleaned/
data/model_ready/
*.parquet
*.feather
*.csv.gz

# But DO keep the canonical processed match dataset committed when ready (override at end of project)
# !data/model_ready/matches.parquet
!data/
!data/cleaned/
!data/cleaned/matches.csv
!data/model_ready/
!data/model_ready/matches.csv

# Soccerdata cache
.soccerdata/
soccerdata_cache/

# Model artefacts (regenerable via `python -m src.train`; too large for git)
results/model_*.joblib
results/model_*.pkl
results/predictions/
# Keep leaderboard.csv tracked for grader convenience.
!results/leaderboard.csv

# Optuna study DB
optuna.db

# Misc
*.log
*.tmp

# Node modules pulled in by slide-rendering tools (Marp, etc.)
node_modules/
slides/node_modules/
*.tsbuildinfo
